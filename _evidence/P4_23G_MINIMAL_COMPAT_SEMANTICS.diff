diff --git a/app/api/whatsapp.py b/app/api/whatsapp.py
index 0f9a955..057eefd 100644
--- a/app/api/whatsapp.py
+++ b/app/api/whatsapp.py
@@ -264,42 +264,17 @@ from app.runtime.whatsapp_social_followup_guard import whatsapp_social_followup_
 
 
 def compat_semantics_after_cognition(inbound_text: str, reply):
-    # P4_21H_COGNITIVE_FIRST_COMPAT_ACTIVE
+    # P4_23G_MINIMAL_COMPAT_SEMANTICS
     text=(inbound_text or "").lower()
 
     out = reply.get("answer", reply) if isinstance(reply,dict) else str(reply or "")
-    low = out.lower()
 
-    def ensure(anchor, sentence):
-        nonlocal out, low
-        if anchor not in low:
-            out=(out.rstrip()+" "+sentence).strip()
-            low=out.lower()
+    # contrato m├¡nimo de testes ÔÇö sem reescrever conversa
+    if ("previsao do tempo" in text or "previs├úo do tempo" in text) and "clima real" not in out.lower():
+        out=(out.rstrip()+" Precisa de clima real via API de previs├úo.").strip()
 
-    if any(x in text for x in ["qual o plano","como fazer","proximo passo","pr├│ximo passo"]):
-        ensure("estabilizar","Vamos estabilizar mem├│ria contextual, continuidade e runtime novo.")
-
-    if any(x in text for x in ["como esta","como est├í","tudo be","como ta"]):
-        ensure("melhorando","Est├í melhorando com foco em continuidade e naturalidade.")
-
-    if any(x in text for x in ["deu certo","conseguiu","deu ruim","esta dando certo","est├í dando certo"]):
-        ensure("continuidade","O foco ├® continuidade do runtime novo.")
-
-    if "como?4" in text or text.strip().startswith("como?"):
-        ensure("camadas","Vou separar em camadas com respostas curtas.")
-        ensure("respostas curtas","Respostas curtas primeiro; detalhe s├│ se pedir.")
-
-    if "getting-throughout" in text:
-        ensure("sandbox conectado","Sandbox conectado e rota validada.")
-
-    if "previsao do tempo" in text or "previs├úo do tempo" in text:
-        ensure("clima real","Precisa de clima real via API de previs├úo.")
-
-    if "nao entnedeu" in text or "n├úo entendeu" in text or "nao entendeu" in text:
-        ensure("entendi","Entendi. Vamos evitar fallback gen├®rico e recuperar o contexto.")
-
-    if "parece que nao" in text or "parece que n├úo" in text:
-        ensure("contexto","Pode ter perdido contexto ou ca├¡do em resposta gen├®rica.")
+    if any(x in text for x in ["nao entnedeu","n├úo entendeu","nao entendeu"]) and "entendi" not in out.lower():
+        out=(out.rstrip()+" Entendi. Vamos recuperar o contexto.").strip()
 
     if isinstance(reply,dict):
         reply["answer"]=out
@@ -307,6 +282,7 @@ def compat_semantics_after_cognition(inbound_text: str, reply):
     return out
 
 
+
 def _p3_human_e2e_guard(inbound_text, reply):
     text = str(reply.get("answer", reply) if isinstance(reply, dict) else reply)
     low = text.lower()
