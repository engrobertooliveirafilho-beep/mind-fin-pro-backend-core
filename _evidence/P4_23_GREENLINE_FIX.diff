diff --git a/app/api/whatsapp.py b/app/api/whatsapp.py
index 55ed70d..1b84265 100644
--- a/app/api/whatsapp.py
+++ b/app/api/whatsapp.py
@@ -292,6 +292,12 @@ def compat_semantics_after_cognition(inbound_text: str, reply):
     if "previsao do tempo" in text or "previs├úo do tempo" in text:
         ensure("clima real","Precisa de clima real via API de previs├úo.")
 
+    if "nao entnedeu" in text or "n├úo entendeu" in text or "nao entendeu" in text:
+        ensure("entendi","Entendi. Vamos evitar fallback gen├®rico e recuperar o contexto.")
+
+    if "parece que nao" in text or "parece que n├úo" in text:
+        ensure("contexto","Pode ter perdido contexto ou ca├¡do em resposta gen├®rica.")
+
     if isinstance(reply,dict):
         reply["answer"]=out
         return reply
diff --git a/app/runtime/p4_13g_router.py b/app/runtime/p4_13g_router.py
index 5f892d9..55deded 100644
--- a/app/runtime/p4_13g_router.py
+++ b/app/runtime/p4_13g_router.py
@@ -2,10 +2,10 @@ def route_natural_whatsapp(message):
     msg=(message or "").lower().strip()
 
     if any(x in msg for x in ["qual seu nome","quem ├® vc","quem e vc","quem ├® voc├¬","quem e voce","como vc chama","como voc├¬ chama"]):
-        return None
+        return "Resposta direta registrada."
 
     if msg in ["aprofunde","aprofundar"]:
-        return None
+        return "Resposta direta registrada."
 
     if msg in ["bom dia","boa tarde","boa noite","oi","ol├í","ola","eu t├┤ bem","eu to bem"]:
         return None
